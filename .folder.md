# Folder: /

1. **地位 (Position)**: 项目根目录，承载本地视频流管理平台的全局入口、规范和部署资产。
2. **逻辑 (Logic)**: 通过 `apps` 承载业务执行，通过 `configs/deploy/scripts` 管理运行环境，通过 `docs` 维护设计与操作规范。
3. **约束 (Constraints)**: 根目录改动必须同步 `README.md` 与 `.cursor/CONSTITUTION.md`，保持结构与文档一致。
