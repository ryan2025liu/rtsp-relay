# 进度更新 - 2026-04-25

## 今日完成

1. 项目治理与规范
   - 完成项目专用开发宪法与 Cursor 规则落地。
   - 完成目录结构规范重写并与项目实际结构对齐。

2. 目录结构初始化
   - 创建了正式开发骨架目录：
     - `apps/api`, `apps/web`, `apps/worker`
     - `packages/common`
     - `configs/*`, `deploy/*`, `docs/*`, `scripts/*`, `tests/*`
     - `runtime/data`, `runtime/tmp`
   - 关键目录补齐 `.folder.md`。

3. 文档体系完善
   - 更新根 `README.md`（项目定位、结构、开发启动方式）。
   - 新增 MVP 开发计划文档。
   - 新增 API v1 概览草案。

4. 代码骨架启动
   - `apps/api`：FastAPI 入口与 `/health`。
   - `apps/worker`：任务管理器基础状态结构（start/stop/status）。
   - `scripts/dev/start-api.sh`：本地启动脚本。

5. 清理与边界确认
   - 已移除旧项目中转目录，仅保留新项目目录内改动。
   - 明确本项目与业务系统只保留运行时契约（`stream_key` + RTMP/SRS）。

## 当前状态评估

1. 进入“可正式开发”状态：规范、结构、最小代码入口已就绪。
2. Sprint 1 目标尚未完成：`sources/jobs` 正式 API 和持久化层尚未实现。

## 下一步开工计划（Sprint 1）

1. API 领域模型与存储
   - 在 `apps/api` 建立 `Source/Target/Profile/Job` Pydantic 模型。
   - 引入 SQLite 持久化（先轻量实现，后续可迁移 PostgreSQL）。

2. 控制面接口首批实现
   - `GET/POST/PUT/DELETE /api/v1/sources`
   - `POST /api/v1/jobs/{source_id}/start`
   - `POST /api/v1/jobs/{source_id}/stop`
   - `GET /api/v1/jobs/{source_id}/status`

3. API 与 worker 联动
   - 将 `apps/worker/manager.py` 接入 API 服务层（先进程内调用）。
   - 统一任务状态与错误返回结构。

4. 基础测试与文档同步
   - 新增单元测试（Source CRUD、Job 状态切换）。
   - 更新 `docs/02-api/v1-overview.md` 为已实现状态。

## 收工说明

今日工作到此结束，下一次直接按“下一步开工计划”从 Sprint 1 第 1 项开始执行。

---

## 补充更新（本地 Git 初始化后继续开发）

1. 已初始化本地 Git 仓库，并完成首个基线提交：
   - `chore: initialize local project baseline`

2. 已完成 Sprint 1 首批后端闭环：
   - `SQLite` 本地持久化落地
   - `Source` CRUD API 实现
   - `Target` 最小 CRUD API 实现
   - `Job start/stop/status` API 实现
    - API 已接入 `apps/worker/manager.py` 的进程内任务管理器
   - 本地 API 容器镜像与 `docker-compose.local.yml` 已补齐

3. 当前实现特点：
   - 默认目标自动初始化为 `default`
   - Source 未传 `target_id` 时自动绑定当前默认目标
   - Job 启动时会实际拉起本地子进程
   - Job 状态在内存管理的同时回写本地数据库快照
   - URL 返回时已做脱敏处理
   - 当前默认目标受保护，不能直接删除
   - 服务重启后会自动恢复 `enabled` 源任务
   - 已提供 `jobs/logs` 接口读取本地 worker 日志，并在返回前脱敏 RTSP 凭据
   - 已提供 `jobs/restart` 接口
   - Worker 异常退出后会按固定延迟做本地自动重试
   - 已提供 `sources/{id}` 聚合详情接口，返回 source/target/job/recent_logs

4. 开发环境修正：
   - `scripts/dev/start-api.sh` 已改为优先使用项目本地 `.venv`
   - 优先选用 Python 3.12，避免 Python 3.14 与 `pydantic-core` 的兼容问题

5. 已验证：
   - `python -m py_compile` 通过
   - `tests/test_api.py` 通过

## 补充更新（本地联调拓扑调整）

1. 本地联调方案已调整为：
   - `relay-api` + `relay-web` 通过 `docker compose -f docker-compose.local.yml up -d --build` 启动
   - SRS 改为复用局域网现成服务，不再在本地 Compose 中额外启动

2. 文档同步项：
   - 更新根 `README.md`
   - 更新 `docs/01-architecture/README.md`
   - 更新 `docs/03-operations/README.md`
   - 新增 ADR 记录本次部署拓扑决策

3. 实际启动验证：
   - `relay-api` 与 `relay-web` 已通过 `docker compose -f docker-compose.local.yml up -d --build` 启动
   - `relay-api` `/health` 返回 `ok`
   - API 已对 LAN origin 放行，响应头包含 `access-control-allow-origin: http://192.168.126.189:5174`
   - Vite 前端已在容器内注入 `VITE_API_BASE_URL=http://192.168.126.189:18081`

4. 播放地址修复：
   - `Target` 增加 `playback_vhost`
   - 前端预览链接已自动追加 `?vhost=vid-2162315`
   - 当前默认目标已回写为 `playback_vhost=vid-2162315`

5. 前端展示增强：
   - 目标卡片标题已直接显示 `playback_vhost`
   - 通道详情区同步展示播放 vhost，便于排查“有流无画面”

6. 本地预览功能：
   - 详情区已接入 HLS 视频预览窗口
   - 预览资源通过 API 同源代理返回，避免浏览器跨域限制

7. 预览交互增强：
   - 播放器增加播放 / 暂停 / 重载控制
   - 增加播放状态与错误提示，便于本地排障

8. 前端容器依赖修复：
   - `relay-web` 容器启动时改为执行 `npm ci`
   - 解决容器内 `hls.js` 依赖未同步导致的导入失败

9. 预览链接 origin 修正：
   - HLS/FLV 预览链接改为显式使用 `VITE_API_BASE_URL`
   - 避免浏览器把预览请求打到前端 origin 上

10. 预览播放器稳定性修正：
    - HLS.js 仅在 fatal 错误时显示失败状态
    - 网络/媒体错误先尝试自动恢复，减少误报

11. RTMP 推流与 `playback_vhost` 对齐：
    - 发现仅把 `vhost` 用在 HTTP 播放、未附加到 ffmpeg 的 RTMP 输出时，在 SRS 多 vhost 场景下可能导致推流失败，进而 HLS 预览无流
    - `apps/worker/manager.py` 增加 `build_rtmp_publish_url`，在 `playback_vhost` 非空时为推流 URL 附加 `?vhost=...`
    - 详情页在日志匹配 `Error opening output` 时展示提示条，并同步展示与后台一致的 RTMP 链接（含 vhost）

12. 内网固定主机部署骨架：
    - 新增 `docker-compose.server.yml`、`deploy/env.server.example`、`apps/web/Dockerfile.prod` + Nginx 静态服务
    - 运维说明：`docs/03-operations/deploy-lan-server.md`；决策：`docs/04-adr/0002-*.md`
