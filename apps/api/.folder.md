# Folder: /apps/api

1. **地位 (Position)**: 控制面后端，提供配置管理、任务编排、状态查询 API。
2. **逻辑 (Logic)**: 接收 Web/API 请求 -> 校验参数 -> 生成任务指令 -> 调用 worker 管理层 -> 返回标准结果。
3. **约束 (Constraints)**: 不直接执行 ffmpeg 命令；所有外部接口变更必须更新 `docs/02-api`。
