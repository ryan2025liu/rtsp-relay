# Folder: /configs

1. **地位 (Position)**: 运行配置中心，管理环境变量模板、转码 profile、进程配置。
2. **逻辑 (Logic)**: 统一提供不同环境和场景下的静态配置，供 api/worker/deploy 读取。
3. **约束 (Constraints)**: 禁止存放真实密钥；所有配置项变更需同步文档与 `.env.example`。
