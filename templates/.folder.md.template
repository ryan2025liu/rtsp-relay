# .folder.md 模板

使用方法：复制此文件到目标目录，重命名为 `.folder.md`，然后填写内容。

---

# Folder: /path/to/your/folder

1. **地位 (Position)**: 
   [描述这个目录在系统中的角色和职责]
   [例：业务逻辑层，负责处理核心业务规则]

2. **逻辑 (Logic)**: 
   [描述核心工作流程和数据流向]
   [例：接收控制器请求 → 调用数据模型 → 执行业务规则 → 返回结果]

3. **约束 (Constraints)**: 
   - [约束1：例：禁止直接处理 HTTP 请求/响应]
   - [约束2：例：必须使用依赖注入获取数据库连接]
   - [约束3：例：所有公共方法必须有错误处理]

---

## 文件说明（可选）

| 文件 | 职责 |
|------|------|
| `example.ts` | [描述文件作用] |
| `another.ts` | [描述文件作用] |

---

## 依赖关系（可选）

```
本目录依赖:
  - ../models/      # 数据模型
  - ../utils/       # 工具函数

被以下目录依赖:
  - ../controllers/ # 控制器层
```

---

*最后更新: [日期]*
